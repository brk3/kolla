{
    "command": "/usr/sbin/rsyslogd -n -i /home/rsyslog/.syslog.pid",
    "config_files": [
        {
            "source": "{{ container_config_directory }}/rsyslog.conf",
            "dest": "/etc/rsyslog.conf",
            "owner": "root",
            "perm": "0644"
        }
    ]
}
